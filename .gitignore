.env
__pycache__/
*.pyc
.DS_Store
.cursor/
.claude/
vercel-api/.vercel/
vercel-api/node_modules/
/node_modules/
/package.json
/package-lock.json
.venv/
.venv_video/
한국거래소/
framer-components/.video_frames/
docs/*.pdf

# Brain 학습 자료 — 저작권 자료 + 28MB, 로컬만
배리티 브레인 학습 도서/
_verify_integration.py

# Auto-trading local artifacts
data/mock_orders.log
data/mock_broker_state.json
data/auto_trade_history.json
data/.auto_trade_paused
data/.system_maintenance_mode
data/.last_user_telegram_ts
data/.timing_state.json
.pytest_cache/

# 실행별 감사 스냅샷 (당일 최종본 data/history/YYYY-MM-DD.json 은 계속 추적)
data/history/runs/
.pr_body_chat_hybrid.md

# Backup / safety-net 산출물 — 1.9MB × 매 cron commit 누적 차단 (2026-04-30, Sprint 11)
# save_portfolio 가 매번 portfolio.json.bak 생성. 로컬 fallback 용도라 git tracked 불필요.
data/portfolio.json.bak
data/metadata/*.bak
data/metadata/*.bak.*
data/*.json.bak
data/*.json.bak.*
.vercel
data/cache/5r_analysis/
data/analysis/_archive/
# ARENA B축 산출물(asset_stats μ/σ 엣지) = 비공개 유지. 버서커 shadow 3파일만 공개
# (검증 trail = win condition 공개 차별점, PM 2026-06-08). dir 내용 ignore + 3파일 카브.
data/arena/*
!data/arena/berserker_status.json
!data/arena/berserker_shadow.jsonl
!data/arena/berserker_shadow_score.json

# P2 추가 (2026-05-02)
logs/
estate/.mplconfig/

# sync 충돌 복제본 (iCloud/Dropbox 등) — 2026-05-02
* [0-9].*
* [0-9]/
* [0-9][0-9].*
* [0-9][0-9]/

# 보안 키 / 복구 토큰 — 2026-05-06
*키*.txt
*recovery*.txt
*recovery*.key
*.pem

# ESTATE backtest dry-run mock (P1) — 실 산출물 X
data/estate_backtest_v0_dry_run/

# 학습 자료 / 외부 리서치 본문 (2026-05-16 보강) — 저작권 + 큰 PDF 로컬만
터미널 보충 학습 자료. /
배리티_브레인_투자_바이블.pdf
# VERITY_ESTATE_status_*.pdf — 일일 status 자동 export, gitignore (로컬만)
VERITY_ESTATE_status_*.pdf
.git-private/
/CLAUDE.md
/docs/
# 소형주 코너 검증 IC 결과 = 비공개(.git-private 추적, 점수 held 2027 RULE 7). 공개 main 차단.
/data/smallcap_corner_ic_history.jsonl
# US fair-value(RIM V/P) 관측 = Sharadar own-use → 공개 재배포 금지. .git-private 추적, 점수 held ~2028.
/data/observations/fair_value_gap_us.jsonl
# 미장 1500 per-ticker 시계열 = 87MB git-bloat 차단(cron broad add sweep 방지). _summary/report 만 커밋.
# 스크리너=us_stock_report_public.json(536K). per-ticker 카드 1500 커버=후속 Blob. 기존 추적 15는 유지.
/data/us_financials/*.json
!/data/us_financials/_summary.json
