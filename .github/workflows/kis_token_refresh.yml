name: KIS Token Refresh (24h guard sweep)

on:
  # 2026-09-06: 하루 한 번 고정 시각 실행은 24h 가드와 GitHub 지연이 겹치면 한 회차를
  # 건너뛰어 토큰 공백이 최대 하루 가까이 생겼다. 2시간마다 확인하되 발급 여부는 기존
  # 24h lock 하나가 결정한다. 실행 횟수와 발급 횟수는 다르며, 하루 두 번 발급은 불가하다.
  # minute=47 은 daily_realtime dispatch 슬롯(00/30)과 분리한다.
  schedule:
    - cron: '47 */2 * * *'
  workflow_dispatch:

# Node.js 24 강제 — 2026-06-02 GitHub Node 20 deprecation 대비 (2026-05-07)
env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"
  # 🚨 RULE 1 — GH Actions 단일 발급원 (PM 2026-05-31). 발급 직후 Supabase publish (Railway/Vercel 소비).
  KIS_SHARED_TOKEN: "1"

permissions:
  contents: write  # data/.kis_issued_date.txt (daily lock) commit/push 용

concurrency:
  # KIS 토큰 중복 발급 방지 — daily_analysis / daily_analysis_full / bond_etf /
  # export_trade 와 동일 group 공유. 타 workflow 가 KIS API 호출 중일 때
  # 대기 → 완료 후 실행. 자정 갱신은 보통 다른 cron 과 겹치지 않지만
  # 드물게 지연 실행 시 중복 토큰 발급 원천 차단.
  group: verity-data-write
  cancel-in-progress: false

jobs:
  refresh:
    runs-on: ubuntu-latest
    timeout-minutes: 5

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install minimal deps
        run: pip install requests

      - name: 현재 날짜 (KST)
        id: date
        run: echo "today=$(TZ=Asia/Seoul date +%Y-%m-%d)" >> $GITHUB_OUTPUT

      - name: KIS 토큰 캐시 복원
        uses: actions/cache@v4
        with:
          path: ~/.cache/verity_kis_token.json
          key: kis-token-v2-${{ runner.os }}-${{ steps.date.outputs.today }}
          restore-keys: |
            kis-token-v2-${{ runner.os }}-

      - name: KIS 토큰 갱신
        id: refresh
        env:
          KIS_APP_KEY: ${{ secrets.KIS_APP_KEY }}
          KIS_APP_SECRET: ${{ secrets.KIS_APP_SECRET }}
          # 추가 계좌 (PM 2026-08-07, 회원 2명). secret 미등록이면 빈 문자열 → 해당 계좌만 건너뛴다.
          # 🚨 오퍼레이터 변수명은 위 그대로 — 이름을 바꾸면 배포 env 를 전부 다시 넣어야 하고
          #    하나라도 빠지면 RULE 1 상 재발급도 못 해 거래가 멈춘다.
          BROKER_SLUGS: ${{ vars.BROKER_SLUGS || 'operator' }}
          KIS_APP_KEY__FRIEND: ${{ secrets.KIS_APP_KEY__FRIEND }}
          KIS_APP_SECRET__FRIEND: ${{ secrets.KIS_APP_SECRET__FRIEND }}
          KIS_OPENAPI_BASE_URL: ${{ secrets.KIS_OPENAPI_BASE_URL || vars.KIS_OPENAPI_BASE_URL }}
          SUPABASE_URL: ${{ secrets.SUPABASE_URL || vars.SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
        run: |
          python - <<'PYEOF'
          import sys, os
          sys.path.insert(0, os.getcwd())
          from api.trading.kis_broker import KISBroker

          def refresh_one(slug):
              """계좌 1개 발급. True=발급함 / False=건너뜀. 가드 로직은 단일 계좌 때와 동일."""
              broker = KISBroker(broker=slug)
              if not broker.is_configured:
                  print(f"[{slug}] KIS 미설정 (APP_KEY/SECRET 없음) — 건너뜀")
                  return False

              # lock 사전 체크 — 시각 기반 (2026-05-13 자정 race fix 후 변경).
              # 🚨 2026-05-22 RULE 1 정정: 6h → 23h. KIS = 1일 1토큰(발급 간격 최소 24h).
              # 🚨 2026-05-29 RULE 1 재정정: 23h → 24h. 사고 5/27 + 5/28 매일 알림.
              # 5/27 00:08 preflight + 23:08 preflight = 23h 0m → 23h 가드 통과 → 2번째 발급.
              # 5/28 22:11 발급 = 다시 ~23h interval → 사용자 카톡 알림 1건 (RULE 1 P0).
              # 24h 가드 = preflight/refresh 어느 쪽이든 정책 (발급 간격 최소 24h) 정합.
              # 2시간 확인 주기 중 24h 를 처음 넘긴 회차만 발급한다.
              # 🚨 계좌마다 락 파일이 분리되어 있어 이 가드도 계좌별로 독립 판정된다.
              if broker._is_recently_issued(hours=24):
                  print(f"[{slug}] lock — 24h 이내 이미 발급됨. KIS API 호출 skip (1일 1토큰 가드).")
                  return False

              # force_refresh=True — broker 내부 24h 가드 bypass. 위 workflow-level 24h 가드가
              # 1일 1토큰 보장 (당일 미발급 시에만 여기 도달 = backup 발급).
              token = broker.authenticate(force_refresh=True)
              print(f"[{slug}] KIS 토큰 갱신 완료: {token[:8]}***")
              return True

          # 🚨 RULE 1 은 **앱키마다** 적용된다. 계좌가 늘어도 한 계좌가 하루 2토큰을 받으면
          #   그 계좌가 제재 대상이다. 그래서 슬러그마다 독립된 락 파일·store 행·24h 가드를
          #   쓰고, 이 루프는 한 계좌 실패가 다른 계좌 발급을 막지 않도록 격리한다.
          slugs = [s.strip() for s in os.environ.get("BROKER_SLUGS", "operator").split(",") if s.strip()]
          if "operator" not in slugs:
              slugs.insert(0, "operator")   # 오퍼레이터 발급 누락 방지 (env 오설정 대비)

          issued, skipped = [], []
          for slug in slugs:
              try:
                  ok = refresh_one(slug)
              except Exception as e:
                  print(f"[{slug}] 발급 실패: {e}")
                  skipped.append(slug)
                  continue
              (issued if ok else skipped).append(slug)
          print(f"발급 {issued} / 건너뜀 {skipped}")

          # 🚨 다음 step 의 자가 신고용. 발급 건수를 넘겨야 "발급했는데 락 커밋 0" 을
          #   실패로 판정할 수 있다 (2026-08-09 사고 = 그 판정이 없어서 조용히 통과).
          _gh_out = os.environ.get("GITHUB_OUTPUT")
          if _gh_out:
              with open(_gh_out, "a", encoding="utf-8") as _f:
                  _f.write(f"issued_count={len(issued)}\n")
          PYEOF

      - name: daily lock 파일 commit & push
        env:
          # 앞 step 의 실발급 건수. 이 값이 있어야 "발급했는데 락 미기록" 을 판정한다.
          ISSUED_COUNT: ${{ steps.refresh.outputs.issued_count }}
        run: |
          git config user.name "AI Stock Bot"
          git config user.email "gywns0126@gmail.com"
          # RULE 4 — 계좌가 늘면 락 파일도 늘어난다. 글롭으로 받아 신규 계좌 추가 시
          #   yml 수정을 잊어 락이 propagate 안 되는 사고(= 러너마다 재발급 = RULE 1 위반)를 막는다.
          #
          # 🚨 2026-08-09 사고 — 반드시 파일별로 나눠서 add 할 것.
          #   한 줄로 묶으면(`git add A B*`) 글롭 B* 가 안 맞을 때 git add 가 **원자적으로
          #   전부 실패**해서 오퍼레이터 락 A 까지 스테이징되지 않는다. FRIEND 계좌 secret 이
          #   미등록이라 __friend 락이 생성되지 않았고, `|| true` 가 그 실패를 삼켜
          #   아래 diff 검사가 "변화 없음"으로 통과했다. 결과 = 락이 8/7 에 동결된 채
          #   8/8·8/9 실발급 2회가 기록되지 않음 → 24h 가드가 stale 락을 보고 항상
          #   통과 → 하루 2토큰 가능 상태(RULE 1 위반 직전).
          git add data/.kis_issued_date.txt || true
          for _lock in data/.kis_issued_date__*.txt; do
            [ -e "$_lock" ] && git add "$_lock" || true
          done
          # 🚨 자가 신고 (2026-08-09 사고의 본체 대응).
          #   발급과 락 커밋은 별개 사건이다. 발급이 있었는데 스테이징이 비면 락이
          #   기록되지 않았다는 뜻이고, 그대로 exit 0 하면 24h 가드가 stale 락을 보고
          #   계속 통과한다 = 알림도 로그 에러도 없이 RULE 1 가드가 꺼진다.
          #   사람이 락 커밋 이력을 대조해야만 잡히던 것을 run 이 스스로 신고하게 한다.
          if git diff --staged --quiet; then
            if [ "${ISSUED_COUNT:-0}" -gt 0 ] 2>/dev/null; then
              echo "::error::발급 ${ISSUED_COUNT}건인데 daily lock 변화 0 — 락 미기록. RULE 1 가드 무력화 상태다 (2026-08-09 사고 재발)."
              exit 1
            fi
            echo "daily lock 파일 변화 없음 — 이번 run 발급 0건 (24h 가드로 skip)"
            exit 0
          fi
          TODAY=$(TZ=Asia/Seoul date +%Y-%m-%d)
          git commit -m "🔐 KIS daily lock ${TODAY}"
          BRANCH="${{ github.ref_name }}"
          for i in 1 2 3; do
            if git push origin "$BRANCH"; then
              echo "✓ daily lock push 성공"
              exit 0
            fi
            echo "push 실패 (attempt $i) — rebase 후 재시도"
            git pull --rebase --autostash origin "$BRANCH" || git merge --abort || true
          done
          echo "::error::daily lock push 3회 실패"
          exit 1
