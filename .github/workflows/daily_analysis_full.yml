name: Stock Analysis Pipeline (Full & Periodic)

# Full/Full_US/periodic_* 모드 전용 워크플로.
# realtime/quick cron(daily_analysis.yml)과 concurrency group 을 분리해서
# 5~10분마다 도는 경량 cron 이 35분짜리 full 실행을 cancel 하지 못하게 한다.

on:
  schedule:
    # 장 마감 full 분석 (KST 16:00 = UTC 07:00), 월~금
    - cron: '7 7 * * 1-5'
    # 미장 마감 full 분석 KST 06:30 (UTC 21:30), 화~금
    # 2026-05-18 fix: 옛 '2-6' (Tue-Sat UTC) 결함 — Sat UTC = KST Sun 06:30 = US 미장
    # 휴장 + KRX 일요일 휴장 = 분석할 세션 0 + KRX 빈 JSON 무한 fail. cron 자체 false trigger.
    # '2-5' (Tue-Fri UTC) = 각각 Wed/Thu/Fri/Sat KST 06:30 = 정상 post-US 마감 분석.
    - cron: '30 21 * * 2-5'
    # 주간 리포트: 매주 토요일 KST 09:00 (UTC 00:00)
    - cron: '7 0 * * 6'
    # 월간 리포트: 매월 1일 KST 09:00 (UTC 00:00)
    - cron: '7 0 1 * *'
    # 분기 리포트: 1/4/7/10월 2일 KST 10:00 (UTC 01:00)
    - cron: '7 1 2 1,4,7,10 *'
    # 반기 리포트: 1/7월 3일 KST 10:00 (UTC 01:00)
    - cron: '7 1 3 1,7 *'
    # 연간 리포트: 1월 4일 KST 10:00 (UTC 01:00)
    - cron: '7 1 4 1 *'
  # 2026-05-18 — Vercel Cron fallback (GitHub Actions schedule silent miss 회피).
  # vercel-api/api/cron/dispatch_pulse.py 가 매분 KST 16:07 / 06:30 시각 매핑 → repository_dispatch.
  repository_dispatch:
    types:
      - daily_analysis_full
  workflow_dispatch:
    inputs:
      mode:
        description: '분석 모드 (full / full_us / periodic_daily / periodic_weekly / periodic_monthly / periodic_quarterly / periodic_semi / periodic_annual)'
        required: false
        default: 'full'
        type: string
      verity_mode:
        description: 'VERITY_MODE (LLM 호출 정책). manual dispatch default = staging (cost 격리). 운영 검증 필요시 prod.'
        required: false
        default: 'staging'
        type: choice
        options:
          - staging
          - prod
          - dev

# Node.js 24 강제 — 2026-06-02 GitHub Node 20 deprecation 대비 (2026-05-07)
env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"
  # 🚨 RULE 1 — GH Actions 단일 발급원 (PM 2026-05-31). 발급 직후 Supabase publish (Railway/Vercel 소비).
  KIS_SHARED_TOKEN: "1"

permissions:
  contents: write

# WARN-17: data/* 쓰기 워크플로 단일 그룹 직렬화 (realtime/quick/bond_etf/export_trade 공유).
# cancel-in-progress: false → full/realtime 이 동시에 매치되면 큐잉.
concurrency:
  group: verity-data-write
  cancel-in-progress: false

jobs:
  analyze:
    runs-on: ubuntu-latest
    timeout-minutes: 120

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          # B-3: brain_evolution.collect_evolution_log 가 git log 30 commit 분석 필요
          fetch-depth: 100

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: 현재 날짜 (KST)
        id: date
        run: echo "today=$(TZ=Asia/Seoul date +%Y-%m-%d)" >> $GITHUB_OUTPUT

      - name: KIS 토큰 캐시 복원 (하루 1회 재발급 보장)
        uses: actions/cache@v4
        with:
          path: ~/.cache/verity_kis_token.json
          key: kis-token-v2-${{ runner.os }}-${{ steps.date.outputs.today }}
          restore-keys: |
            kis-token-v2-${{ runner.os }}-

      - name: Check optional trade/patent API secrets
        env:
          KIPRIS_API_KEY: ${{ secrets.KIPRIS_API_KEY || vars.KIPRIS_API_KEY }}
          KIPRIS_ACCESS_KEY: ${{ secrets.KIPRIS_ACCESS_KEY || vars.KIPRIS_ACCESS_KEY }}
          PUBLIC_DATA_API_KEY: ${{ secrets.PUBLIC_DATA_API_KEY || vars.PUBLIC_DATA_API_KEY }}
        run: |
          if [ -z "$KIPRIS_API_KEY" ] && [ -z "$KIPRIS_ACCESS_KEY" ]; then
            echo "::warning::KIPRIS: KIPRIS_API_KEY 또는 KIPRIS_ACCESS_KEY 없음"
          fi
          if [ -z "$PUBLIC_DATA_API_KEY" ]; then
            echo "::warning::관세청: PUBLIC_DATA_API_KEY 없음"
          fi
          KRX_API_KEY="${{ secrets.KRX_API_KEY || vars.KRX_API_KEY }}"
          if [ -z "$KRX_API_KEY" ]; then
            echo "::warning::KRX: KRX_API_KEY 없음 → docs/KRX_OPEN_API_SETUP.md"
          fi

      - name: Ensure Korean font for PDF
        run: |
          mkdir -p api/reports/fonts
          if [ ! -f api/reports/fonts/NanumGothic.ttf ]; then
            curl -sL -o api/reports/fonts/NanumGothic.ttf \
              "https://github.com/google/fonts/raw/main/ofl/nanumgothic/NanumGothic-Regular.ttf"
            curl -sL -o api/reports/fonts/NanumGothicBold.ttf \
              "https://github.com/google/fonts/raw/main/ofl/nanumgothic/NanumGothic-Bold.ttf"
          fi

      - name: Detect periodic schedule
        id: detect_mode
        run: |
          CRON="${{ github.event.schedule }}"
          MANUAL="${{ github.event.inputs.mode }}"
          DAY_OF_MONTH=$(TZ=Asia/Seoul date +%d)
          if [ -n "$MANUAL" ]; then
            echo "mode=$MANUAL" >> $GITHUB_OUTPUT
          elif [ "$CRON" = "7 1 4 1 *" ]; then
            echo "mode=periodic_annual" >> $GITHUB_OUTPUT
          elif [ "$CRON" = "7 1 3 1,7 *" ]; then
            echo "mode=periodic_semi" >> $GITHUB_OUTPUT
          elif [ "$CRON" = "7 1 2 1,4,7,10 *" ]; then
            echo "mode=periodic_quarterly" >> $GITHUB_OUTPUT
          elif [ "$CRON" = "7 0 1 * *" ] || ([ "$CRON" = "7 0 * * 6" ] && [ "$DAY_OF_MONTH" = "01" ]); then
            echo "mode=periodic_monthly" >> $GITHUB_OUTPUT
          elif [ "$CRON" = "7 0 * * 6" ]; then
            echo "mode=periodic_weekly" >> $GITHUB_OUTPUT
          elif [ "$CRON" = "7 7 * * 1-5" ]; then
            echo "mode=full" >> $GITHUB_OUTPUT
          elif [ "$CRON" = "30 21 * * 2-5" ]; then
            echo "mode=full_us" >> $GITHUB_OUTPUT
          else
            echo "mode=full" >> $GITHUB_OUTPUT
          fi

      # KIS [C] 보강 (commit bac4ec2 재활성화 조건) —
      # KIS 토큰을 분석 시작 전에 발급하고 daily lock 을 즉시 push.
      # 기존 흐름: analysis step (28분) 안에서 발급 → 끝나야 commit/push 발생.
      #           그 사이 KIS Token Refresh cron 이 stale lock 보고 재발급 위험 (5/3 사고 원인).
      # 본 step: lock 파일을 ~수초 안에 origin 에 push → 다음 cron 의 fresh checkout 이 즉시 인지.
      - name: KIS preflight (issue + immediate lock push)
        env:
          KIS_APP_KEY: ${{ secrets.KIS_APP_KEY }}
          KIS_APP_SECRET: ${{ secrets.KIS_APP_SECRET }}
          KIS_OPENAPI_BASE_URL: ${{ secrets.KIS_OPENAPI_BASE_URL || vars.KIS_OPENAPI_BASE_URL }}
          SUPABASE_URL: ${{ secrets.SUPABASE_URL || vars.SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
        run: |
          if [ -z "$KIS_APP_KEY" ]; then
            echo "[KIS preflight] secrets 미설정 — skip"
            exit 0
          fi
          python - <<'PYEOF'
          import sys, os
          sys.path.insert(0, os.getcwd())
          try:
              from api.trading.kis_broker import KISBroker
              broker = KISBroker()
              if not broker.is_configured:
                  print("[KIS preflight] 미설정 — skip")
                  sys.exit(0)
              # 2026-05-13 자정 race fix: date 비교 → 시각 기반 interval
              # 2026-05-29 RULE 1 정정: 23h → 24h (5/27 00:08+23:08 / 5/28 22:11 매일 알림 사고)
              if broker._is_recently_issued(hours=24):
                  print("[KIS preflight] 24h 이내 발급 — skip")
                  sys.exit(0)
              # force_refresh=False — broker 24h interval 가드 (자정 race 차단) 작동
              token = broker.authenticate(force_refresh=False)
              print(f"[KIS preflight] 발급 완료: {token[:8]}***")
          except Exception as e:
              # silent — 본 analysis step 에서 재시도 가능
              print(f"[KIS preflight] 실패 (analysis step 에서 재시도): {e}")
              sys.exit(0)
          PYEOF

      - name: Daily lock immediate push (KIS [C])
        if: always()
        env:
          KIS_APP_KEY: ${{ secrets.KIS_APP_KEY }}
        run: |
          if [ -z "$KIS_APP_KEY" ]; then
            echo "[lock push] KIS 미설정 — skip"
            exit 0
          fi
          if [ ! -f data/.kis_issued_date.txt ]; then
            echo "[lock push] 파일 없음 — preflight 발급 skip 케이스. 종료"
            exit 0
          fi
          git config user.name "AI Stock Bot"
          git config user.email "gywns0126@gmail.com"
          git add data/.kis_issued_date.txt
          if git diff --staged --quiet; then
            echo "[lock push] 변화 없음 — origin 이미 동기"
            exit 0
          fi
          TODAY=$(TZ=Asia/Seoul date +%Y-%m-%d)
          git commit -m "🔐 KIS daily lock ${TODAY} (preflight)"
          BRANCH="${{ github.ref_name }}"
          for i in 1 2 3; do
            git rebase --abort 2>/dev/null || true
            if git pull --rebase --autostash -X ours origin "$BRANCH" && git push origin "$BRANCH"; then
              echo "[lock push] ✓ 성공 (시도 $i)"
              exit 0
            fi
            echo "[lock push] 시도 $i 실패 — 재시도"
            sleep $((i * 5))
          done
          echo "::warning::[lock push] 3회 실패 — 분석 끝 commit step 에서 재시도"

      - name: Refresh macro snapshot (신선도 보장 — GH */30 schedule throttle 우회)
        # macro_collect cron(*/30) 이 GH schedule throttle 로 실제 ~5h 간격 실행 →
        # full 분석 시점 snapshot 이 자주 stale (feedback_gh_short_cron_silent_skip).
        # 분석-크리티컬 경로를 flaky schedule 에서 분리: 읽기 직전 inline 갱신 →
        # main.py fast-path HIT 보장. 실패해도 main.py 가 stale snapshot 으로 degrade
        # (commit 9c505e439) → continue-on-error 안전. macro = KIS 무관 (RULE 1 N/A).
        continue-on-error: true
        env:
          FRED_API_KEY: ${{ secrets.FRED_API_KEY || vars.FRED_API_KEY }}
          KOSIS_API_KEY: ${{ secrets.KOSIS_API_KEY || vars.KOSIS_API_KEY }}
          ECOS_API_KEY: ${{ secrets.ECOS_API_KEY || vars.ECOS_API_KEY }}
          NEWS_API_KEY: ${{ secrets.NEWS_API_KEY }}
          ALPHA_VANTAGE_API_KEY: ${{ secrets.ALPHA_VANTAGE_API_KEY }}
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: python -m api.builders.macro_collect_builder

      - name: Run analysis
        env:
          # 2026-05-11: VERITY_MODE 명시 — schedule cron = prod, manual dispatch = input (default staging).
          # cost 감축 (개발 manual 호출 시 LLM 자동 mock).
          VERITY_MODE: ${{ github.event.inputs.verity_mode || 'prod' }}
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          TELEGRAM_ALLOWED_CHAT_IDS: ${{ secrets.TELEGRAM_ALLOWED_CHAT_IDS }}
          VAMS_INITIAL_CASH: ${{ vars.VAMS_INITIAL_CASH || '10000000' }}
          VAMS_MAX_PER_STOCK: ${{ vars.VAMS_MAX_PER_STOCK || '2000000' }}
          ANALYSIS_MODE: ${{ steps.detect_mode.outputs.mode }}
          DART_API_KEY: ${{ secrets.DART_API_KEY }}
          FRED_API_KEY: ${{ secrets.FRED_API_KEY }}
          ECOS_API_KEY: ${{ secrets.ECOS_API_KEY }}
          KIPRIS_API_KEY: ${{ secrets.KIPRIS_API_KEY || vars.KIPRIS_API_KEY }}
          KIPRIS_ACCESS_KEY: ${{ secrets.KIPRIS_ACCESS_KEY || vars.KIPRIS_ACCESS_KEY }}
          PUBLIC_DATA_API_KEY: ${{ secrets.PUBLIC_DATA_API_KEY || vars.PUBLIC_DATA_API_KEY }}
          KRX_API_KEY: ${{ secrets.KRX_API_KEY || vars.KRX_API_KEY }}
          KRX_OPENAPI_KEY: ${{ secrets.KRX_API_KEY || vars.KRX_API_KEY }}
          # Phase 2-A — 유니버스 ramp-up (결정 4). secrets 미설정 시 빈 문자열 → config.py default 500.
          # UNIVERSE_RAMP_UP_AUTO 는 워크플로 미주입 — config.py default False 유지 (자동 ramp-up 금지).
          UNIVERSE_RAMP_UP_STAGE: ${{ secrets.UNIVERSE_RAMP_UP_STAGE }}
          # Phase 2-B — wide_scan shadow mode. 2026-05-18 PM 옵션 C 결정 확정 — SHADOW default.
          # 활성값: SHADOW (jsonl 적재만) / CANARY_5 (5%만 적용 — step d) / PRODUCTION (전면 — 65 거래일 검증 후)
          # PRODUCTION 게이트 = 자기 산식 8/10 + 학술 N≥30 둘 다 통과.
          WIDE_SCAN_MODE: ${{ vars.WIDE_SCAN_MODE || secrets.WIDE_SCAN_MODE || 'SHADOW' }}
          GEMINI_MODEL_CRITICAL: ${{ vars.GEMINI_MODEL_CRITICAL || 'gemini-2.5-pro' }}
          GEMINI_PRO_ENABLE: ${{ vars.GEMINI_PRO_ENABLE || '1' }}
          GEMINI_CRITICAL_TOP_N: ${{ vars.GEMINI_CRITICAL_TOP_N || '3' }}
          CLAUDE_MODEL_LIGHT: ${{ vars.CLAUDE_MODEL_LIGHT || 'claude-haiku-4-5' }}
          CLAUDE_MODEL_DEFAULT: ${{ vars.CLAUDE_MODEL_DEFAULT || 'claude-sonnet-4-6' }}
          CLAUDE_MODEL_HEAVY: ${{ vars.CLAUDE_MODEL_HEAVY || 'claude-opus-4-7' }}
          CLAUDE_OPUS_ENABLE: ${{ vars.CLAUDE_OPUS_ENABLE || '1' }}
          CLAUDE_TOP_N: ${{ vars.CLAUDE_TOP_N || '2' }}
          CLAUDE_MIN_BRAIN_SCORE: ${{ vars.CLAUDE_MIN_BRAIN_SCORE || '70' }}
          CLAUDE_IN_QUICK: ${{ vars.CLAUDE_IN_QUICK || '0' }}
          CLAUDE_IN_REALTIME: ${{ vars.CLAUDE_IN_REALTIME || '0' }}
          CLAUDE_QUICK_TOP_N: ${{ vars.CLAUDE_QUICK_TOP_N || '1' }}
          CLAUDE_EMERGENCY_THRESHOLD_PCT: ${{ vars.CLAUDE_EMERGENCY_THRESHOLD_PCT || '5.0' }}
          CLAUDE_EMERGENCY_COOLDOWN_MIN: ${{ vars.CLAUDE_EMERGENCY_COOLDOWN_MIN || '120' }}
          CLAUDE_TAIL_RISK_VERIFY: ${{ vars.CLAUDE_TAIL_RISK_VERIFY || '0' }}
          CLAUDE_MORNING_STRATEGY: ${{ vars.CLAUDE_MORNING_STRATEGY || '0' }}
          DEADMAN_FAIL_THRESHOLD: ${{ vars.DEADMAN_FAIL_THRESHOLD || '3' }}
          REPORT_SEND_HOUR_KST: ${{ vars.REPORT_SEND_HOUR_KST || '16' }}
          REPORT_SEND_MINUTE_KST: ${{ vars.REPORT_SEND_MINUTE_KST || '30' }}
          POSTMORTEM_ENABLED: ${{ vars.POSTMORTEM_ENABLED || '1' }}
          FINNHUB_API_KEY: ${{ secrets.FINNHUB_API_KEY }}
          POLYGON_API_KEY: ${{ secrets.POLYGON_API_KEY }}
          POLYGON_TIER: ${{ vars.POLYGON_TIER || 'free' }}
          NEWS_API_KEY: ${{ secrets.NEWS_API_KEY }}
          NEWSAPI_MAX_ARTICLES: ${{ vars.NEWSAPI_MAX_ARTICLES || '5' }}
          SEC_EDGAR_USER_AGENT: ${{ secrets.SEC_EDGAR_USER_AGENT }}
          PERPLEXITY_API_KEY: ${{ secrets.PERPLEXITY_API_KEY }}
          KIS_APP_KEY: ${{ secrets.KIS_APP_KEY }}
          KIS_APP_SECRET: ${{ secrets.KIS_APP_SECRET }}
          KIS_ACCOUNT_NO: ${{ secrets.KIS_ACCOUNT_NO }}
          KIS_OPENAPI_BASE_URL: ${{ secrets.KIS_OPENAPI_BASE_URL || vars.KIS_OPENAPI_BASE_URL }}
          SUPABASE_URL: ${{ secrets.SUPABASE_URL || vars.SUPABASE_URL }}
          SUPABASE_ANON_KEY: ${{ secrets.SUPABASE_ANON_KEY || vars.SUPABASE_ANON_KEY }}
          # SERVICE_ROLE_KEY: api/health.py _check_reports_signed_url 가 signed URL 발급 검증할 때 필요 (write 권한).
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
        run: python api/main.py

      - name: 공개 공시속보 피드 빌드 (public-safe)
        # main.py STEP 5.78 이 data/dart_catalyst_alerts.jsonl 갱신 후 → public-safe JSON 변환.
        # 순수 변환 (외부호출/KIS 0). 다음 step git add data/ (broad) 자동 포함 = RULE 4.
        continue-on-error: true
        run: python -m api.builders.public_disclosure_feed_builder

      - name: KRX 시총 스냅샷 (PER/PBR 자체계산 입력)
        # KRX OpenAPI 공식 시총·상장주식수 → data/krx_mktcap.json. KIS 무관(KRX_API_KEY). 빌더 직전 실행.
        # 네트워크 호출. 다음 stock_report 빌더가 읽어 PER/PBR=시총÷순익·자본 자체계산.
        # 🚨 KRX_API_KEY env 필수 — 누락 시 krx_openapi 가 ("",[]) 반환→0행→기존 파일 동결(6/19~6/23 stale 사고). RULE 8.
        continue-on-error: true
        env:
          KRX_API_KEY: ${{ secrets.KRX_API_KEY || vars.KRX_API_KEY }}
        run: python -m api.builders.krx_mktcap_snapshot

      - name: 공개 종목 리포트 빌드 (public-safe, allowlist)
        # recommendations.json → RULE7 allowlist strip → data/stock_report_public.json.
        # 순수 변환 (krx_mktcap.json·sector_map 읽음, KIS 0). 다음 step git add data/ (broad) 자동 = RULE 4.
        # NAVER_* = 지분구조 인물 링크 생존성 검증 (빌더 _naver_news_total — 신규 인물만,
        # person_link_cache.json 커밋 캐시 1차 · 키 부재 = 플래그 생략 안전 강등).
        continue-on-error: true
        env:
          NAVER_CLIENT_ID: ${{ secrets.NAVER_CLIENT_ID }}
          NAVER_CLIENT_SECRET: ${{ secrets.NAVER_CLIENT_SECRET }}
        run: python -m api.builders.stock_report_public_builder

      - name: AI 사실 종합 빌드 (출처 기반, 평가·추천 0)
        # stock_report_public.json 사실 → Gemini flash-lite 가 자연스럽게 다듬기만(grounded) → data/ai_synthesis.json.
        # 🚨 RULE 6 escape hatch(자기 trail 위 종합) + RULE 7/유사투자자문: verdict 단어 post-filter, 걸리면 결정론 fallback.
        # 키 없으면 결정론 종합. 다음 step git add data/ (broad) 자동 = RULE 4. publish-data 등재 완료.
        continue-on-error: true
        run: python -m api.builders.ai_synthesis_builder

      # 공개 가격 스냅샷·랭킹 보드 빌드 step = 2026-07-03 시세 재배포 컴플라이언스 Phase 2 로 은퇴 (KRX raw 발행 중단, 네이버 link-out/TV 위젯 대체)

      - name: 국민연금 보유종목 집계 (NPS 공개 패널)
        # data.go.kr #15106890(국민연금 대량보유, serviceKey=PUBLIC_DATA_API_KEY 공통) → 전체 5%+ ~114종목.
        # 키 없으면 DART portfolio.json institutional_holders 운영풀로 graceful. → data/nps_holdings.json.
        # 순수 변환(KIS 0). RULE 7 = 5%+ 대량보유 공시 사실(지분율)만, 점수·추천 0. 다음 step git add data/ (broad) 자동 = RULE 4. publish allowlist 등재 완료.
        continue-on-error: true
        env:
          PUBLIC_DATA_API_KEY: ${{ secrets.PUBLIC_DATA_API_KEY || vars.PUBLIC_DATA_API_KEY }}
        run: python -m api.collectors.nps_holdings

      - name: 증권 대차잔고 집계 (공매도 압력 proxy, 금융위 공개패널)
        # data.go.kr #1160100(금융위 대차거래, serviceKey=PUBLIC_DATA_API_KEY 공통) → 전종목 ~2,769개 대차잔고.
        # 진짜 공매도잔고=KRX 봇차단으로 free 불가 → 대차잔고(빌려준 주식=공매도 재료) proxy. 키 없으면 graceful skip. → data/securities_lending.json.
        # 순수 fetch(KIS 0). RULE 7 = 대차잔고 금액·수량 사실만(점수·추천·스퀴즈예측 0). 다음 step git add data/ (broad) 자동 = RULE 4. publish allowlist 등재 완료.
        continue-on-error: true
        env:
          PUBLIC_DATA_API_KEY: ${{ secrets.PUBLIC_DATA_API_KEY || vars.PUBLIC_DATA_API_KEY }}
        run: python -m api.collectors.securities_lending

      - name: 미장 공시속보 빌드 (SEC 8-K)
        # us_financials 유니버스 8-K 수집 → data/us_disclosure_feed.json. SEC EDGAR(무료·UA 내장, KIS 0).
        # 외부 fetch. 다음 step git add data/ (broad) 자동 포함 = RULE 4.
        continue-on-error: true
        run: python -m api.builders.us_disclosure_feed_builder

      - name: 공개 공시 forensics 빌드 (이벤트 이력·빈도, public-safe)
        # dart_catalyst_alerts.jsonl → 종목별 유상증자/감자/CB/정정 이벤트 빈도·타임라인 → data/disclosure_forensics.json.
        # 순수 변환(외부호출/KIS 0). RULE 7 = report_nm 사실·빈도만, 자체 위험점수 0. 다음 step git add data/ (broad) = RULE 4.
        continue-on-error: true
        run: python -m api.builders.disclosure_forensics_builder

      - name: 공개 KR forensics 빌드 (특수관계자 거래·우발부채·소송 사실)
        # dart_related_party_cache + dart_litigation_cache (by_ticker) → data/kr_forensics_public.json.
        # 순수 변환(외부호출 0). RULE 7/6 = 원문 사실 리스트만, 위험점수·심각도·터널링 의심서술·요약 0. 다음 step git add data/ (broad) = RULE 4.
        continue-on-error: true
        run: python -m api.builders.kr_forensics_public_builder

      - name: 공개 원자재 노출 빌드 (원자재→KR 노출 산업/종목)
        # kr_sector_map(yfinance industry) ⋈ stock_report_public(name·시총) → data/commodity_exposure.json.
        # 순수 변환(외부호출/KIS 0). RULE 7 = 산업 멤버십 사실만, 수혜·추천·점수 0. 다음 step git add data/ (broad) = RULE 4.
        continue-on-error: true
        run: python -m api.builders.commodity_exposure_public_builder

      - name: 공개 시장경보 빌드 (KRX 투자주의/경고/위험·단기과열·관리종목)
        # recommendations KR → KIS inquire-price output 경보 플래그 → data/market_warnings.json.
        # 🚨 KISBroker(cache_only=True) — 신규 토큰 발급 절대 금지(RULE 1), 공유 토큰 read 전용. RULE7 = KRX 공식 지정 사실만.
        # 🚨 2026-06-27 P0 fix: KIS env 누락 → cache app_key mismatch(env empty) → 공유토큰 read 실패 → 전종목 skip →
        #   market_warnings 6/20~6/27 동결(7.4일, 경보 0건). cache_only 라 발급 0(RULE1 안전), env 는 캐시키 매칭용. insider DART 누락과 동일 클래스.
        continue-on-error: true
        env:
          KIS_APP_KEY: ${{ secrets.KIS_APP_KEY }}
          KIS_APP_SECRET: ${{ secrets.KIS_APP_SECRET }}
          KIS_OPENAPI_BASE_URL: ${{ secrets.KIS_OPENAPI_BASE_URL || vars.KIS_OPENAPI_BASE_URL }}
          # 🚨 2026-07-10 진짜 fix — 공유 토큰 read 는 SUPABASE 2종 필수(_kis_shared_enabled).
          #   6/27 fix 가 KIS 키만 추가하고 이 2종 누락 → file cache stale 시 전량 skip 지속
          #   (6/20~7/10 3주 동결, run 은 success = 침묵 실패). preflight step 과 동일 세트.
          SUPABASE_URL: ${{ secrets.SUPABASE_URL || vars.SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
        run: python -m api.builders.market_warnings_public_builder

      - name: 공개 내부자거래 빌드 (DART elestock, 美 Form4 KR판)
        # 전 종목(stock_report universe 1,635) rotation+carry-forward+rate-limit 가드(020/021)+budget.
        # DART 무료 키 재사용(KIS 0, ~1.6K 콜 ≪ 20K). RULE7 = 공시 사실만. 다음 step git add data/ (broad) = RULE 4.
        # 🚨 2026-06-27 P0 fix: DART_API_KEY env 누락으로 builder "부재 — skip" → insider_trades.json 6/20~6/27 동결(6.7일).
        #   continue-on-error 가 silent 마스킹. krx_mktcap KRX_API_KEY 누락(fab598aa2, 6/19~23 동결)과 동일 클래스.
        continue-on-error: true
        env:
          DART_API_KEY: ${{ secrets.DART_API_KEY }}
        run: python -m api.builders.insider_trades_public_builder

      - name: 공개 종목 수급 빌드 (외국인·기관 5일, public-safe)
        # 전 종목 시총순 rotation, 일별 캡(FLOW_MAX_TICKERS, anti-bot) + carry-forward → ~6일 내 전 종목.
        # 검증된 파서(flow_observation_logger: 모바일 trend JSON→frgn HTML 폴백). 네이버 외부 fetch(KIS 0).
        # RULE 7 = 외국인/기관 순매매량(외부 사실)만. 다음 step git add data/ (broad) = RULE 4.
        continue-on-error: true
        env:
          FLOW_MAX_TICKERS: '300'
        run: python api/builders/stock_flow_public_builder.py

      - name: KR 종목 관측 신호 raw 적재 (v0, RULE 7 관측 only · 주간)
        # 내부자·수급·희석·총수일가 raw → data/observations/kr_cross_section_observations.jsonl (금요일만 self-run, 점수 0).
        # 엔진 미사용 신호 forward IC trail 누적(N≥252 2027 검증용). spec docs/kr_cross_section_observation_spec_v0.
        # 공개 빌더(insider/flow/forensics/stock_report) 뒤. KIS 0. broad git add data/ = RULE 4(observations 디렉토리 자동).
        continue-on-error: true
        run: python -m api.builders.kr_observation_logger

      - name: 업종 진입 뷰 빌드 (섹터 집계, public-safe)
        # stock_report+kr_sector_map+krx_mktcap+stock_flow_5d 순수 transform (외부호출/KIS 0). 위 수급 step 뒤.
        # RULE 7 = 섹터별 사실 중앙값·대표주·수급. 다음 broad git add data/ = RULE 4.
        continue-on-error: true
        run: python -m api.builders.sector_overview_builder


      - name: 수급 종합 빌드 (공매도·신용잔고, public-safe)
        # portfolio.json kis_snapshots 추출(공매도 FHPST04830000·신용 FHPST04760000). 🚨 KIS 호출 0=RULE 1 무관(로컬 읽기).
        # portfolio.json(main 분석 산출) 뒤. RULE 7 = KRX/KIS 외부 사실(공매도 비중·신용율)만. broad git add data/ = RULE 4.
        continue-on-error: true
        run: python -m api.builders.supply_demand_public_builder

      - name: 입구 지도 빌드 (공개 자산 count·기준시각 집계, public-safe)
        # 발행 중 공개 JSON 들의 count/generated_at 만 추출한 초경량 지도(~1KB). 외부호출/KIS 0 (로컬 읽기).
        # 위 빌더 step 들 전부 뒤(당일 산출 집계). RULE 7 = 사실(개수·시각)만. broad git add data/ = RULE 4. publish-data 등재 완료.
        continue-on-error: true
        run: python -m api.builders.entrance_map_builder

      - name: 커버리지(채움율) 감사 — 백필 회귀 감시
        # 리포트 필드·동반 파일 채움율 일일 측정 → data/metadata/coverage_report.json(+history.jsonl).
        # 신선도 감사와 직교("얼마나 채워져 있나"). 회귀 >10%p = WARN 로그. 외부호출 0. broad git add data/ = RULE 4.
        continue-on-error: true
        run: python -m api.builders.coverage_report_builder

      - name: 관점 지도 빌드 (욕구·경기 체질·자사주 — 분류·집계 사실)
        # 업종 사실→욕구 계층 / 매출 YoY 변동성 실측 3분위 / 자기주식 공시 건수. 외부호출 0.
        # RULE 7 = 점수·랭킹 0, 기준 공개. broad git add data/ = RULE 4. publish-data 등재 완료.
        continue-on-error: true
        run: python -m api.builders.perspective_maps_builder

      - name: Brain audit baseline (PRE/POST 측정 영속화)
        # 2026-05-18 확정 — docs/BRAIN_SCORE_AUDIT_20260518.md audit cycle 영속화.
        # portfolio.json 의 brain_score 분포 + component fill + grade + quadrant 측정.
        # 결과: data/metadata/brain_audit.jsonl append (다음 step 의 git add data/ 자동 포함).
        # fail 시 workflow continue (continue-on-error: true).
        continue-on-error: true
        run: python scripts/audit_brain_score_baseline.py

      - name: Brain audit telegram push (자리 비움 시 핸드폰 알림)
        # 2026-05-18 c-fix — workflow 자체 telegram push (Engineer wait/cron 의존 회피).
        # send_message 가 quiet hours (23:00~07:00 KST) + dedupe 자동 정합.
        # 16:07 KST 평일 daily_full = 통과 / 06:30 KST = quiet hours suppress.
        continue-on-error: true
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          TELEGRAM_QUIET_HOURS_ENABLED: ${{ vars.TELEGRAM_QUIET_HOURS_ENABLED || '1' }}
        run: python scripts/push_brain_audit_telegram.py

      - name: Generate predictions (Prediction Layer v0)
        # 2026-06-01 — descriptive→predictive. recommendations.json + macro_industry_alignment.json
        # → data/metadata/prediction_trail.jsonl (forward-only cross-section). 채점은 별도 cron.
        # 다음 step 의 git add data/ (broad) 자동 포함 = RULE 4 정합.
        # graceful: 입력 결손 시 exit 0 (파이프라인 fail 안 시킴). spec docs/prediction_layer_spec_v0_2026_06_01.md.
        continue-on-error: true
        run: python scripts/generate_predictions.py

      - name: Score predictions (Prediction Layer v0 채점 cron)
        # 2026-06-01 — generate 직후 채점 (생성 → 채점 순서). eval_date 도달분(scored==false)을
        # 실현 시장 결과(history snapshot 가격 diff)로 채점 → trail scored=true rewrite +
        # data/prediction_ic_history.jsonl 집계 append. 다음 step git add data/ (broad) 자동 포함 = RULE 4.
        # 첫 단기 eval = 6/8 (created 6/1 + 7일). 그 전엔 도달분 0 = graceful. spec §9.3.
        # stock 정식 채점 / sector 보류(change_pct=heat 메트릭, return source 미검증).
        # 2026-06-13 — ML(XGB up_probability) shadow trail 동시 채점 (별 trail/ic_history, source-agnostic scorer 재사용).
        #   ai_upside_relax override 정당성 검증 자료 누적. 관측 only. spec docs/ml_shadow_prediction_spec_v0_2026_06_13.md.
        # 2026-06-13 — 관측-only 신호(AAII/NAAIM/FINRA/Form4) market-level trail 채점 (별 trail/ic_history,
        #   market-level scorer = snapshot market_summary index level diff 재사용). "N≥252 wire 가치" 판정 자료
        #   누적. 관측 only — 결정 피드백 0. spec docs/observation_signal_trails_spec_v0_2026_06_13.md.
        continue-on-error: true
        run: |
          python scripts/score_predictions.py
          python scripts/score_predictions.py --trail data/metadata/ml_prediction_trail.jsonl --ic-history data/ml_prediction_ic_history.jsonl
          # 2026-06-13 — 국면(regime) 시계열 채점 (별 trail/ic_history, market-level 단일값 → 시계열 NW scorer).
          #   macro_override/cycle_stage 의 forward 시장(KOSPI primary + S&P) 예측력 검증 자료 누적. 관측 only(RULE 7).
          #   spec docs/regime_validation_spec_v0_2026_06_13.md. 채점 도달 전엔 graceful 0.
          python scripts/score_predictions.py --regime --trail data/metadata/regime_prediction_trail.jsonl --ic-history data/regime_prediction_ic_history.jsonl
          python scripts/score_observations.py

      - name: Build validation summary (forward trail 검증 상태 집계 — 게이트 읽기 준비)
        # 2026-06-13 — score 직후 실행 (채점 → 집계 순서). 기존 *_ic_history 산출물(brain/ml/
        # factor) + trail 원본(shadow/sector)을 read-only 재집계 → data/validation_summary.json.
        # 신규 산식 0 / 결정 피드백 0 (RULE 7). 모든 항목 "가설(N=X)" 라벨 + hit_rate 병기.
        # N=252 IC 게이트(2027-05) 읽기 준비 + 운영자 크레덴셜. 다음 step git add data/ (broad) = RULE 4.
        # graceful: 입력 결손 시 exit 0. spec docs/validation_summary_spec_v0_2026_06_13.md.
        continue-on-error: true
        run: python scripts/build_validation_summary.py

      - name: ARENA 버서커 shadow (검증 진행도 status + 보수 vs 버서커 trail)
        # 2026-06-08 — recommendations.json 생성 후 실행. berserker_status.json(공격 다이얼+잠금
        # 카운트다운) + berserker_shadow.jsonl(일별 목표 포트폴리오 trail) + score.json 산출.
        # 다음 step git add data/ (broad) + gitignore 카브(data/arena/* + 버서커 3파일 !) = RULE 4 정합.
        # shadow only(실자본 0). 입력 결손 시 graceful. spec [[project_arena_kickoff_2026_05_30]] 6/8.
        continue-on-error: true
        run: |
          python -m api.arena.berserker_status
          python -m api.arena.berserker_shadow

      - name: Commit & push results
        run: |
          git config user.name "AI Stock Bot"
          git config user.email "gywns0126@gmail.com"
          git add data/
          HOUR_KST=$(TZ=Asia/Seoul date +%H:%M)
          MODE="${{ steps.detect_mode.outputs.mode }}"
          if [[ "$MODE" == periodic_* ]]; then
            LABEL=$(echo "$MODE" | sed 's/periodic_//')
            git diff --staged --quiet || git commit -m "📋 ${LABEL} 리포트 $(TZ=Asia/Seoul date +%Y-%m-%d)"
          else
            git diff --staged --quiet || git commit -m "📊 [${MODE}] 분석 업데이트 $(TZ=Asia/Seoul date +%Y-%m-%d) ${HOUR_KST}"
          fi
          BRANCH="${{ github.ref_name }}"
          PUSHED=false
          for i in 1 2 3 4 5; do
            git rebase --abort 2>/dev/null || true
            git merge --abort 2>/dev/null || true
            if git pull --rebase -X theirs origin "$BRANCH" && git push origin "$BRANCH"; then
              PUSHED=true
              break
            fi
            echo "Push attempt $i failed, retrying in $((i * 10))s..."
            sleep $((i * 10))
          done
          if [ "$PUSHED" != "true" ]; then
            echo "::error::All 5 push attempts failed"
            exit 1
          fi

      - name: Publish hot data to VERITY-data
        # Phase A: dual-write — main 에도 commit (위), gh-pages 에도 force-orphan push.
        # 검증 후 Phase B 에서 fetch URL 마이그레이션, Phase C 에서 main commit 제거.
        if: always()
        uses: ./.github/actions/publish-data
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          personal_token: ${{ secrets.VERITY_DATA_PAT }}
          blob_token: ${{ secrets.VERCEL_BLOB_TOKEN }}
