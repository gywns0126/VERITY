/**
 * ⚠️ DEPRECATED (2026-05-04 Card→Bell 단일화 결정)
 *
 * UserActionBell (FAB 52x52, 빨간 배지, 호버 패널) 으로 흡수.
 * Framer 빌드 호환 stub. 페이지 인스턴스 제거 후에도 캐시된 module 참조가
 * 남을 수 있어 영구 보존 (메모리 feedback_framer_stubs_permanent).
 *
 * 본 컴포넌트는 invisible — return null. 화면 영향 0.
 */
import { addPropertyControls } from "framer"
import * as React from "react"

const UserActionQueueCard: React.FC = () => null

export default UserActionQueueCard

addPropertyControls(UserActionQueueCard, {})
