/**
 * ⚠️ DEPRECATED (2026-05-05 Plan v0.1 §3 폐기 결정)
 *
 * Framer 빌드 호환 stub. 사용자 framer 측에서 코드 컴포넌트 등록 해제 후
 * git rm 가능.
 */
import * as React from "react"

export default function CompareCard(): React.ReactElement | null {
    return null
}
