.portfolio.lock

# 백업 파일 (strategy_registry.json.bak 등)
*.bak

# 매 실행 재생성되는 생성물
weight_optimization_result.json
brain_results_*.json
backfill_replay_result.json
component_ic_result.json
dart_kr_backfill_result.json
dart_kr_ic_result.json

# DART API 캐시 (per corp_code × year, 수백 개 — 재실행 시 자동 재생성)
dart_kr_cache/

# ReportScout 다운로드 PDF (수백 MB 누적 가능 — 재실행 시 url 에서 재다운로드)
reports/

# ReportScout 결과 JSON (매 실행 갱신)
analyst_reports.json

# report_summarizer Gemini 캐시 + 집계 (매 실행 누적/갱신)
report_summaries.json
