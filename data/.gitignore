.portfolio.lock

# 백업 파일 (strategy_registry.json.bak 등)
*.bak

# 매 실행 재생성되는 생성물
weight_optimization_result.json
brain_results_*.json
backfill_replay_result.json
component_ic_result.json
dart_kr_backfill_result.json
dart_kr_ic_result.json

# DART API 캐시 (per corp_code × year, 수백 개 — 재실행 시 자동 재생성)
dart_kr_cache/
# 2026-05-30 — _meta/ 만 git 추적 (5/29 sprint 6338a682 dart_meta_backfill)
# git re-include 제약 (parent dir 통째 ignore = child re-include 불가) →
# workflow yml 안 `git add -f data/dart_kr_cache/_meta/` 사용 (force flag)

# ReportScout 다운로드 PDF (수백 MB 누적 가능 — 재실행 시 url 에서 재다운로드)
reports/

# ReportScout 결과 JSON (매 실행 갱신)
analyst_reports.json

# report_summarizer Gemini 캐시 + 집계 — 의도적으로 git 추적 (un-ignore).
# 2026-05-20 fix: gitignore 시 cron clean checkout 마다 _processed_hashes 빈 dict →
#   run 당 ≤30 만 처리, 누적 0, 같은 PDF Gemini 재과금 → analyst_report 24/25 @50 fallback.
#   git 추적해야 _processed_hashes 가 run 간 persist → 커버리지 누적 + Gemini dedup 절감.
#   무한 증가는 SUMMARY_RETENTION_DAYS prune 로 bound (report_summarizer.py).
# report_summaries.json

# dart_report_analyzer Gemini 분석 캐시 ({ticker: {year: result}}) — 의도적으로 git 추적.
# 2026-05-20 fix: report_summaries.json 와 동일 root cause — gitignore 시 cron clean
#   checkout 마다 _load_cache 빈 dict → 누적 0 + Gemini 재분석 → dart_health 22/25 @50.
#   per-ticker×year 구조라 bound 됨 (prune 불필요).
# dart_analysis_cache.json

# scout_penny.py 가 NASDAQ trader 에서 7일 캐시로 재다운로드
us_tickers.txt

# OHLCV 분석 캐시 (재실행 시 재생성)
cache/

# 5r feasibility / ATR Phase 0 일일 분석 산출물 (재실행 시 재생성)
# 영구 보관 필요한 baseline 은 5/16 verdict 시점에 docs/atr_phase0/ 로 명시 이동
analysis/5r_feasibility_*.json
analysis/5r_full_v2_*.json
analysis/5r_full_v2_*.log
analysis/r_multiple_outliers*
