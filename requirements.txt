pykrx>=1.0.45,<2.0.0
yfinance>=1.2.0,<2.0.0
pandas>=2.0.0,<3.0.0
requests>=2.31.0,<3.0.0
feedparser>=6.0.0,<7.0.0
beautifulsoup4>=4.12.0,<5.0.0
google-genai>=1.0.0,<2.0.0
python-telegram-bot>=21.0,<23.0
python-dotenv>=1.0.0,<2.0.0
xgboost>=2.0.0,<3.0.0
lightgbm>=4.0.0,<5.0.0
catboost>=1.2.0,<2.0.0
scikit-learn>=1.3.0,<2.0.0
# scipy — prediction_scoring 직접 사용(spearmanr/binomtest/t-dist). scikit-learn 전이 의존이나 직접 import 라 명시 pin.
scipy>=1.10,<2.0.0
# Portfolio optimization (Markowitz / Black-Litterman / HRP / DiscreteAllocation).
# v1.6.0 (2026-02) = GC.OS 거버넌스 인수 후 첫 메이저. v2.x breaking change 위험 → pin.
# wiring: DiscreteAllocation spike 우선 (KIS lot 분배), BL/EF/HRP = Brain v6 / Tier 2 trigger.
PyPortfolioOpt==1.6.0
cvxpy>=1.4.0,<2.0.0
# 2026-05-18 fix — CI Python 3.12 정합 (universe_scan run 25996101722 source build fail):
#   numba 0.58.x = Python <3.12 만 → wheel cache miss 시 source build → _guard_py_ver fail.
#   numba 0.59+ (2024-01) = Python 3.12 정식 지원 + PyPortfolioOpt/cvxpy 호환 정합.
#   로컬 Python 3.9 도 numba 0.59 호환 (numba 0.59 = python_requires >=3.9,<3.13).
numpy>=1.26,<3.0
numba>=0.59,<0.62
# Audit 2026-05-17 — 3 spike 도입 (vectorbt 는 Python 3.10+ venv 분리 trigger 대기):
#   pandera = DataFrame schema (jsonl 적재 hook 사전 차단, RULE 4 정합)
#   empyrical-reloaded = VAMS validation 확장 (Sortino/Calmar/Beta/capture, Stefan Jansen 활성 fork)
#   statsforecast = macro 시계열 forecast (AutoARIMA/AutoETS, Brain 보조 input)
pandera>=0.20.0,<1.0.0
empyrical-reloaded>=0.5.10,<1.0.0
statsforecast>=2.0.0,<3.0.0
# 2026-05-30 — A sprint Phase 1 Sub-spec 2 도입 (인프라, RULE 7 quota 미적용):
#   datasketch = MinHash + LSH 24h sliding window novelty 분류 (산식 layer X, 인프라 only)
#   MIT license, numpy 의존만, Python 3.7~3.13 호환.
#   cross-link: [[project_news_impact_3axis_sprint_2026_05_30]] Sub-spec 2 / Perplexity Q3 정합
datasketch>=1.6.0,<2.0.0
dart-fss>=0.4.3,<1.0.0
anthropic>=0.40.0,<1.0.0
fpdf2>=2.8.0,<3.0.0
exchange_calendars>=4.5.0,<5.0.0
fear_and_greed>=0.0.8,<1.0.0
cot_reports>=0.1.0,<1.0.0
# V6 optional: HMM 레짐 감지 (미설치 시 규칙 기반 fallback)
# hmmlearn>=0.3.0,<1.0.0
pdfplumber>=0.11.0,<1.0.0
